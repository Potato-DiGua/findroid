<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.jdtech.jellyfin">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Jellyfin"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ui.activities.player.IjkPlayerActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|uiMode"
            android:exported="true"
            android:theme="@style/TranslucentTheme"
            android:screenOrientation="portrait">
            <intent-filter>
                <data
                    android:host="www.findroid.com"
                    android:path="/ijkplayer"
                    android:scheme="findroid" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.player.PlayerActivity"
            android:screenOrientation="userLandscape" />
        <activity
            android:name=".ui.activities.main.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.JellyfinSplashScreen"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>